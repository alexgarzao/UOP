entity start
	method constructor()
		var int x;
		for(x=1; x <= 30; x=x+1)
			io.writeln("x=", x);
			if (x >= 1 and x <= 10)
				io.writeln("x entre 1 e 10");
			else
				if (x >= 11 and x <= 20)
					io.writeln("x entre 11 e 20");
				else
					io.writeln("x > 20");
				end
			end
		end
	end
end
