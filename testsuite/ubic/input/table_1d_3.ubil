entity start
	method constructor()
		var table tab;
		var int index;

		for(index=1; index<=10; index=index+1)
			tab[index] = index*10;
		end

		for(index=1; index<=10; index=index+1)
			io.writeln("tab[", index, "]=", tab[index]);
		end
	end
end

